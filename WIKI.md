# Slitherlink 求解器 — 算法详解与项目全貌

> 本文档为 [README.md](README.md) 的补充，覆盖地图构建、CSP / SAT 求解器内部原理、图片识别流水线、在线自动求解机制及完整测试数据。

---

## 地图构建方法

求解器把棋盘抽象为一张**平面图（Planar Graph）**，包含三类元素：顶点、边、面。

### 1. 顶点（Vertex）—— 黑点

每个黑点用一个二维坐标 `(r, c)` 表示：
- `0 <= r <= R`，`0 <= c <= C`
- `R` 为格子行数，`C` 为格子列数
- 例如 5×5 的格子棋盘共有 6×6 = 36 个黑点

### 2. 边（Edge）—— 两点之间的可能连线

边分为两种：

| 边类型 | 表示 | 数量 | 连接的两个点 |
|---|---|---|---|
| 横向边 `h[r][c]` | `h` | `(R+1) × C` | `(r, c)` 与 `(r, c+1)` |
| 纵向边 `v[r][c]` | `v` | `R × (C+1)` | `(r, c)` 与 `(r+1, c)` |

每条边在求解过程中有三种状态：
- `-1`（`UNKNOWN`）：尚未确定
- `0`（`CROSS`）：×，不属于回路
- `1`（`LINE`）：连线，属于回路

### 3. 面（Face）—— 格子

每个格子 `(r, c)` 被四条边包围：上边 `h[r][c]`，右边 `v[r][c+1]`，下边 `h[r+1][c]`，左边 `v[r][c]`。

格子内的数字 `n` 约束：这四条边中恰好有 `n` 条状态为 `1`。

---

## CSP 求解器（solver.py）

Slitherlink 属于**约束满足问题（CSP, Constraint Satisfaction Problem）**，求解采用两阶段策略：

1. **约束传播（Constraint Propagation）**：根据数字、回路规则和局部模式，反复推导必为线或必为×的边。
2. **回溯搜索（Backtracking DFS）**：当传播无法继续时，选择一条未知边进行假设-验证；若出现矛盾则回溯。

### 约束类型

#### A. 数字约束（`_propagate_numbers`）

对编号为 `n` 的格子，设其四边中已确定为线的数量 = `line`，已确定为×的数量 = `cross`，未知边数量 = `unknown = 4 - line - cross`：

- 若 `line > n` 或 `cross > 4 - n`，矛盾
- 若 `line == n`，其余未知边 → `×`
- 若 `line + unknown == n`，所有未知边 → `线`

#### B. 点度数约束（`_propagate_dots`）

回路是简单闭合环，每个黑点的度数只能是 `0`（不在回路上）或 `2`（在回路上）。设其相邻边中已确定线的数量 = `line`，未知 = `unknown`：

| 条件 | 推论 |
|---|---|
| `line > 2` | 矛盾 |
| `line == 1` 且 `unknown == 0` | 矛盾（度数为 1） |
| `line == 2` | 其余未知边 → `×` |
| `line == 1` 且 `unknown == 1` | 该未知边 → `线` |
| `line == 0` 且 `unknown == 1` | 该未知边 → `×` |

#### C. 局部定式（`_propagate_basic_patterns`）

求解器实现了经过验证的局部定式：

**水平/垂直相邻 3-3**：当两个标 `3` 的格子相邻时，它们外侧的边必为 LINE。

```
[3][3]  →  左外侧竖边 = LINE，右外侧竖边 = LINE
 3
 3     →  上外侧横边 = LINE，下外侧横边 = LINE
```

**角-3 定式**：当数字 3 出现在棋盘角格时，其靠边的两条外侧边必为 LINE。

**3-顶点-外侧 定式**：当一个顶点连接三个数字 3 时，该顶点的外侧边必为 LINE。

> ⚠️ 对角 3-3 规则（"内角边必为 LINE"）在部分谜题配置下会导致错误推断，因此**未包含**在当前实现中。新模式的加入需经过充分验证。

### 早期环路检测（`_check_cycle` + `RollbackDSU`）

避免无用搜索的关键机制：

1. **增量成环检测**：每次 `set(LINE)` 时，通过可回滚并查集 `RollbackDSU` 检查新边是否使两个已连通的顶点相连。若相连则标记 `_cycle_detected = True`。
2. **早熟环判定**：传播结束后若 `_cycle_detected`，遍历 LINE 边连通分量——若某分量中所有顶点度数 = 2、边数 ≥ 4 且小于总 LINE 边数，说明形成了不完整的小环，抛出 `Contradiction`。
3. **最终验证**：`is_valid_solution()` 用 BFS 验证 LINE 边形成**恰好一个**连通分量。

### 回溯搜索

1. **分支选择**：`find_unknown()` 遍历未知边，通过 `_edge_priority()` 评分选择最优分支。
2. **假设-传播**：先 LINE 后 CROSS，每次尝试后立即约束传播。
3. **状态缓存**：`_dead_cache` 序列化已探明无解的状态，避免重复搜索子树。

### 解的验证

当所有边被赋值后验证三条：数字格 LINE 数匹配、黑点度数 0 或 2、LINE 边构成恰好一个闭合回路。

### 性能优化

针对 15×15 及以上大图，CSP 求解器在以下方面进行了深度优化：

#### 增量数据结构

| 结构 | 类型 | 作用 | 优化前 | 优化后 |
|---|---|---|---|---|
| `_unknown_edges` | `set[tuple]` | 维护所有未知边 | `find_unknown()` 全盘扫描 | 仅迭代未知边集合 |
| `_line_count` | `int` | LINE 边总数 | `_check_cycle()` 每次遍历 | O(1) 读取 |

两者在 `set()` / `restore_state()` 中自动增量更新，支持状态回滚。

#### 分支启发式（`_edge_priority`）

| 因素 | 权重 | 说明 |
|---|---|---|
| 与编号格子相邻 | +20 | 有数字的格子更能触发约束传播 |
| 紧迫度 | +(`urgency` × 15) | `(num - line_count) / unknown` |
| 已知信息量 | +(`known` × 5) | 相邻格子中已确定的边越多越可靠 |
| 0 或 3 格子 | +10 | 极端数字约束力更强 |
| 端点有 1 条 LINE | +30 | 可能延续回路，优先级最高 |
| 端点有 2 条 LINE | −50 | 度数已满，该边只能是 CROSS |

#### 脏标记增量传播

`_dirty_cells` 和 `_dirty_dots` 只记录受最近 `set()` 影响的格子和点，传播复杂度从 O(RC) 降为 O(受影响单元数)。

#### 可回滚并查集（`RollbackDSU`）

支持 `save()` / `restore()`，回溯时 O(1) 恢复，无需深拷贝整个棋盘。

#### 状态去重缓存（`_dead_cache`）

将完整棋盘序列化为 `bytes`（每条边 1 字节），已探明无解的状态直接剪枝。

#### 对角线 0-3 模式

相邻对角格子分别为 0 和 3 时可确定大量边，将部分 15×15 谜题的推理步数从 ~200 降至 ~50。

---

## SAT 求解器（sat_solver.py）

20×20 以上大谜题，CSP 的回溯搜索可能耗时较长。SAT 求解器将 Slitherlink 编码为布尔可满足性问题，使用 Glucose3（CDCL 算法）高效求解。

### 编码策略

#### 1. 预传播（`_pre_propagate`）

编码前先跑一轮 CSP 约束传播固定所有可推导边，大幅减少 SAT 变量数。30×25（750 格）原始约 1800 个变量，预传播后通常仅 ~600。预传播失败直接返回 UNSAT。

#### 2. 顶点度数编码

- 用 `CardEnc.atmost()` 约束每个顶点度数 ≤ 2
- 通过 LINE 边连通相邻顶点的传递闭包编码单回路约束
- 迭代添加回路禁止子句直至解满足单一回路要求

#### 3. 性能基准

| 谜题 | 尺寸 | 网格数 | SAT 变量（预传播后） | 耗时 |
|---|---|---|---|---|
| `5_1.png` | 5×5 | 25 | ~30 | 0.013s |
| `10_1.png` | 10×10 | 100 | ~80 | 0.004s |
| `15_1.png` | 15×15 | 225 | ~120 | 0.008s |
| `20_1.png` | 20×20 | 400 | ~180 | 0.020s |
| `25_1.png` | 30×25 | 750 | ~300 | 0.048s |
| `July.png` | 50×40 | 2000 | ~600 | 0.180s |

---

## 图片识别流水线（image_parser.py）

### 整体流程

```
输入图片
  → 灰度化 + Otsu 二值化
  → 提取连通组件 → 分离黑点（圆形）与数字笔画（非圆形）
  → 黑点聚类 → 拟合网格线（方法1）
      └─ 失败 → 数字位置反推网格（方法2）
  → 逐格 Otsu 二值化 + 角落去噪
  → 两轮数字识别 → 输出二维数字网格
```

### 网格检测

1. **方法1：黑点拟合**：检测圆形黑点 → 对 (x,y) 坐标分别聚类 → 得到均匀网格线。适合黑点清晰、无严重倾斜的图片。
2. **方法2：数字反推**：黑点不足（< 4 个）时，用数字组件中心坐标拟合网格，对反光、模糊图片更鲁棒。

### 两轮数字识别

#### 第一轮：决策树（DT）主导 + 合成 NCC 纠错

```
逐格处理：
  1. 孔洞检测 → 数字 0（高置信度）
  2. 纵横比 < 0.68 → 数字 1（窄长形状）
  3. x_shift < -0.75 → 数字 2（对角线笔画）
  4. 默认归类为 3 → 合成模板 NCC 二次确认
     └─ NCC 说不等于 3 且分数 > 0.30 → 采纳 NCC 结果（纠错）
```

合成模板由 PIL 数学渲染生成，**永不受污染**。仅在 DT 最不可靠的"默认=3"分支用 NCC 纠错。

#### 第二轮：原生 NCC 模板匹配

从第一轮结果构建原生模板（每个数字取样本均值，≥2 个即可构建），按置信度层次决策：极高置信度信任 NCC，中等置信度优先第一轮，低置信度回退第一轮。

---

## 在线自动求解器（auto_solver.py / auto_gui.py）

基于 Playwright 的端到端自动化，对 [cn.puzzle-loop.com](https://cn.puzzle-loop.com) 完成截题 → 求解 → 注入 → 验证的全流程。

### 架构

```
auto_gui.py (Tkinter GUI)
    │
    └→ auto_solver.py (核心引擎)
         │
         └→ Playwright → Chromium / Firefox / WebKit
              │
              └→ puzzle-loop.com
                   ├── Game.task         ← 15×15 数字网格
                   ├── Game.setCellState ← 逐边设置（旧）
                   └── Game.currentState ← 直接赋值（新）
```

### 端到端流程

```
1. 打开浏览器 → 访问网址
2. 登录（可选）→ POST puzzles-mobile.com/api/login
3. 提取 Game.task → 二维数字数组
4. SAT 求解 → solution.h / solution.v（0.01~0.05s）
5. 直接赋值注入
   Game.currentState.cellHorizontalStatus = [[...]]  # (R+1)×C
   Game.currentState.cellVerticalStatus   = [[...]]  # R×(C+1)
   → Game.drawCurrentState() → Game.check()
6. 等待 clock.solved 确认 → 截图 → 浏览器保持打开
```

步骤 3–4 可对已加载的当前题目执行，也可先点击「新题目」按钮获取新题后再执行。

### 注入优化：从 480 次调用到 2 次赋值

**旧方案**：逐边调用 `Game.setCellState()`，15×15 谜题需 480 次 JS 函数调用：

```javascript
Game.setCellState({type: "horizontal", row: 0, col: 0}, 1); // ×480
```

**新方案**：直接操作 `Game.currentState` 内部二维数组，一次完成：

```javascript
// 2 次数组赋值替代 480 次函数调用
Game.currentState.cellHorizontalStatus = [[0,1,0,...], ...]; // 16×15
Game.currentState.cellVerticalStatus   = [[1,0,2,...], ...]; // 15×16
```

值映射：求解器 `1=LINE` → Game `1=LINE`，求解器 `0=CROSS` → Game `2=CROSS`。

### 题目验证与提交流程

puzzle-loop.com 采用**双层验证**：

| 层 | 位置 | 触发 |
|---|---|---|
| ① `Game.check()` | 浏览器 JS | 注入后立即调用，比对 currentState 与正确答案 |
| ② 服务端 MD5 | 网站后端 | 用户点击"完成"→ POST `ansH`（480 字符 y/n 串）|

服务端验证逻辑：`MD5(谜题ID + serializeSolution()) == hashedSolution`。必须注入正确答案才能通过两层验证。

### GUI 功能

`auto_gui.py` 提供 Tkinter 界面：
- 谜题尺寸选择（5×5 ~ 每月谜题）
- 浏览器引擎选择（Chromium / Firefox / WebKit）
- 可见/无头模式切换
- 账号登录（puzzles-mobile.com），会话持久化到 `session.json`
- 双按钮：「求解当前题目」/「获取新题目并求解」
- 实时日志输出，线程安全队列轮询

---

## 应用到的算法与技术

| 算法/技术 | 应用于 | 说明 |
|---|---|---|
| 约束传播 | CSP 求解器 | 数字、度数、局部模式快速推导 |
| 回溯搜索（DFS） | CSP 求解器 | 假设-验证，死状态缓存剪枝 |
| SAT 编码（布尔逻辑） | SAT 求解器 | 边状态 → 布尔变量，约束 → 子句 |
| Glucose3（CDCL） | SAT 求解器 | 大规模高效求解引擎 |
| 基数约束编码（CardEnc） | SAT 求解器 | 顶点度数 ≤ 2 的 AtMost 约束 |
| 回路消除迭代 | SAT 求解器 | 逐步禁止短回路直至单回路 |
| 可回滚并查集 | CSP 求解器 | O(1) 环路检测与回溯恢复 |
| 启发式分支排序 | CSP 求解器 | 综合紧急性、连续性评分 |
| 脏标记增量更新 | CSP 求解器 | O(受影响单元) 替代 O(RC) |
| Playwright 自动化 | 在线求解 | 浏览器操控、JS 注入、表单提交 |
| 归一化互相关（NCC） | 图片识别 | 模板匹配数字分类 |
| 连通组件分析 | 图片识别 | 黑点/数字笔画分离 |
| Otsu 自适应阈值 | 图片识别 | 全局 + 逐格二值化 |
| K-means 聚类 | 图片识别 | 网格线拟合（坐标聚类） |
| BFS / DFS | CSP 求解器 | 最终解验证：单一回路检查 |

---

## 完整测试结果

所有测试谜题的端到端结果（识别 + SAT 求解）：

| 谜题 | 尺寸 | 网格数 | 数字分布 | 求解耗时 |
|---|---|---|---|---|
| `5_1.png` | 5×5 | 25 | 1:2, 2:4, 3:7 | 0.013s |
| `10_1.png` | 10×10 | 100 | 1:12, 2:11, 3:18 | 0.004s |
| `15_1.png` | 15×15 | 225 | 0:3, 1:25, 2:44, 3:27 | 0.008s |
| `15_2.png` | 15×15 | 225 | 0:2, 1:14, 2:43, 3:30 | 0.008s |
| `20_1.png` | 20×20 | 400 | 0:9, 1:26, 2:67, 3:56 | 0.020s |
| `25_1.png` | 30×25 | 750 | 0:6, 1:73, 2:158, 3:95 | 0.048s |
| `July.png` | 50×40 | 2000 | 0:37, 1:258, 2:368, 3:203 | 0.180s |

---

## 可扩展方向

- 🔬 **更多验证过的局部定式**：边界 1-3 相邻、2-2 对角等，需充分测试
- 🌲 **分支策略优化**：conflict-directed backjumping 减少无效回溯
- 🎨 **图片识别增强**：倾斜校正、透视变换、多字体风格
- 🔄 **交互式编辑**：GUI 支持点击输入数字、手动标记边，实时验证
- 🌐 **在线求解增强**：支持更多网站、批量刷题、成绩统计
- 📱 **Web 界面**：Flask / FastAPI 后端 + 前端渲染
- 🏆 **更大规模**：MaxSAT 或 ILP 编码处理 100×100 级别谜题
