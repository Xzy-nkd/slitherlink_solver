# Slitherlink 求解器 — 算法详解与性能分析

> 本文档为 [README.md](README.md) 的补充，包含地图构建、求解器内部原理、图片识别流水线、性能优化细节及完整测试数据。

---

## 地图构建方法

求解器把棋盘抽象为一张**平面图（Planar Graph）**，包含三类元素：顶点、边、面。

### 1. 顶点（Vertex）—— 黑点

每个黑点用一个二维坐标 `(r, c)` 表示：
- `0 <= r <= R`，`0 <= c <= C`
- `R` 为格子行数，`C` 为格子列数
- 例如 5×5 的格子棋盘共有 6×6 = 36 个黑点

### 2. 边（Edge）—— 两点之间的可能连线

边分为两种：

| 边类型 | 表示 | 数量 | 连接的两个点 |
|---|---|---|---|
| 横向边 `h[r][c]` | `h` | `(R+1) × C` | `(r, c)` 与 `(r, c+1)` |
| 纵向边 `v[r][c]` | `v` | `R × (C+1)` | `(r, c)` 与 `(r+1, c)` |

每条边在求解过程中有三种状态：
- `-1`（`UNKNOWN`）：尚未确定
- `0`（`CROSS`）：×，不属于回路
- `1`（`LINE`）：连线，属于回路

### 3. 面（Face）—— 格子

每个格子 `(r, c)` 被四条边包围：
- 上边：`h[r][c]`
- 右边：`v[r][c+1]`
- 下边：`h[r+1][c]`
- 左边：`v[r][c]`

格子内的数字 `n` 约束：这四条边中恰好有 `n` 条状态为 `1`。

---

## CSP 求解器（solver.py）

Slitherlink 属于**约束满足问题（CSP, Constraint Satisfaction Problem）**，求解采用两阶段策略：

1. **约束传播（Constraint Propagation）**：根据数字、回路规则和局部模式，反复推导必为线或必为×的边。
2. **回溯搜索（Backtracking DFS）**：当传播无法继续时，选择一条未知边进行假设-验证；若出现矛盾则回溯。

### 约束类型

#### A. 数字约束（`_propagate_numbers`）

对编号为 `n` 的格子，设其四边中：
- 已确定为线的数量 = `line`
- 已确定为×的数量 = `cross`
- 未知边数量 = `unknown = 4 - line - cross`

推理规则：
- 若 `line > n` 或 `cross > 4 - n`，矛盾。
- 若 `line == n`，则其余未知边必须全部为 `×`。
- 若 `line + unknown == n`，则所有未知边必须全部为 `线`。

#### B. 点度数约束（`_propagate_dots`）

回路是简单闭合环，因此每个黑点在线图中的度数只能是 `0`（不在回路上）或 `2`（在回路上）。

对点 `(r, c)`，设其相邻边中已确定为线的数量 = `line`，未知边数量 = `unknown`：

| 条件 | 推论 |
|---|---|
| `line > 2` | 矛盾 |
| `line == 1` 且 `unknown == 0` | 矛盾（度数为 1） |
| `line == 2` | 其余未知边 → `×` |
| `line == 1` 且 `unknown == 1` | 该未知边 → `线`（必须达到度数 2） |
| `line == 0` 且 `unknown == 1` | 该未知边 → `×`（只剩一条，无法达到度数 2） |

#### C. 局部定式（`_propagate_basic_patterns`）

在基本约束之外，求解器还实现了经过验证的局部定式：

**水平/垂直相邻 3-3**：当两个标 `3` 的格子相邻时，它们外侧的边必为 LINE。

```
[3][3]  →  左外侧竖边 = LINE，右外侧竖边 = LINE
 3
 3     →  上外侧横边 = LINE，下外侧横边 = LINE
```

**角-3 定式**：当数字 3 出现在棋盘角格时，其靠边的两条外侧边必为 LINE。

**3-顶点-外侧 定式**：当一个顶点连接三个数字 3 时，该顶点的外侧边必为 LINE。

> ⚠️ 注意：对角 3-3 规则（"内角边必为 LINE"）在部分谜题配置下会导致错误推断，因此**未包含**在当前实现中。新模式的加入需经过充分验证。

### 早期环路检测（`_check_cycle` + `RollbackDSU`）

这是避免无用搜索的关键机制：

1. **增量成环检测**：每次 `set(LINE)` 时，通过可回滚并查集 `RollbackDSU` 检查新边是否使两个已连通的顶点相连。若相连，标记 `_cycle_detected = True`。

2. **早熟环判定**：在每次约束传播结束后，若 `_cycle_detected` 为 True，则遍历所有 LINE 边构成的连通分量。若某个分量中**所有顶点度数 = 2** 且**边数 ≥ 4** 且**边数 < 总 LINE 边数**，说明形成了一个不完整的小环（早熟环），抛出 `Contradiction` 触发回溯。

3. **最终验证**：`is_valid_solution()` 在所有边确定后用 BFS 验证 LINE 边形成**恰好一个**连通分量。

### 回溯搜索

当约束传播陷入停滞时，进入搜索阶段：

1. **分支选择**：`find_unknown()` 遍历所有未知边，通过 `_edge_priority()` 评分，选择得分最高的边。
2. **假设-传播**：先尝试 LINE、后尝试 CROSS，每次尝试后立即进行约束传播。
3. **状态缓存**：`_dead_cache` 记录已探明无解的状态（序列化为 bytes），避免重复搜索。

### 解的验证

当所有边被赋值后，验证三条：
1. 每个编号格子的四边中 LINE 数量等于其数字。
2. 每个黑点的度数为 `0` 或 `2`。
3. 所有 LINE 边构成**恰好一个**闭合回路。

---

## SAT 求解器（sat_solver.py）

对于 20×20 以上的大谜题，CSP 求解器的回溯搜索可能耗时较长。SAT 求解器将 Slitherlink 编码为布尔可满足性问题（SAT），使用高效的 SAT 引擎（Glucose3）在多项式时间内求解。

### 编码策略

#### 1. 预传播（`_pre_propagate`）

在编码前先运行一轮 CSP 约束传播，固定所有可立即推导的边。这大幅减少了 SAT 变量的数量：
- 30×25（750 格）原始需要约 1800 个变量，预传播后通常只需约 600 个。
- 预传播失败（矛盾）直接返回 UNSAT，无需启动 SAT 引擎。

#### 2. 顶点度数编码

核心约束：每个顶点的度数必须为 0 或 2。传统方法对每个顶点编码"恰好 0 或 2 条边"需要复杂的基数约束。本实现利用**回路连通性**的等价条件：

- 使用 `CardEnc.atmost()` 约束每个顶点度数 ≤ 2。
- 通过"每个 LINE 边连通相邻顶点"的传递闭包编码单回路约束。
- 迭代添加回路禁止子句（loop elimination）直至解满足单一回路要求。

#### 3. 性能

| 谜题 | 尺寸 | 网格数 | SAT 变量（预传播后） | 求解耗时 |
|---|---|---|---|---|
| `5_1.png` | 5×5 | 25 | ~30 | 0.013s |
| `10_1.png` | 10×10 | 100 | ~80 | 0.004s |
| `15_1.png` | 15×15 | 225 | ~120 | 0.008s |
| `20_1.png` | 20×20 | 400 | ~180 | 0.020s |
| `25_1.png` | 30×25 | 750 | ~300 | 0.048s |
| `July.png` | 50×40 | 2000 | ~600 | 0.180s |

---

## 图片识别流水线（image_parser.py）

### 整体流程

```
输入图片
  → 灰度化 + Otsu 二值化
  → 提取连通组件
  → 分离黑点（圆形）与数字笔画（非圆形）
  → 黑点聚类 → 拟合网格线（方法1）
      └─ 失败 → 数字位置反推网格（方法2）
  → 逐格 Otsu 二值化 + 角落去噪
  → 两轮数字识别（见下）
  → 输出二维数字网格
```

### 网格检测

1. **方法1：黑点拟合**：检测圆形黑点 → 对 (x,y) 坐标分别聚类 → 得到均匀网格线。适用于黑点清晰、无严重倾斜的图片。
2. **方法2：数字反推**：当黑点检测不足（< 4 个）时，使用数字组件的中心坐标拟合网格。对反光、模糊图片更鲁棒。

### 两轮数字识别

数字识别是整个流水线的核心，采用**两轮策略**避免级联模板污染：

#### 第一轮：决策树（DT）主导 + 合成 NCC 纠错

```
逐格处理：
  1. 孔洞检测 → 数字 0（高置信度）
  2. 纵横比 < 0.68 → 数字 1（窄长形状，高置信度）
  3. x_shift < -0.75 → 数字 2（对角线笔画，高置信度）
  4. 默认归类为 3 → 用合成模板 NCC 二次确认
     └─ NCC 说不等于 3 且分数 > 0.30 → 采纳 NCC 结果（纠错）
```

关键设计：合成模板由 PIL 数学渲染生成，**永不受污染**。仅在 DT 最不可靠的"默认=3"分支用 NCC 纠错，避免 NCC 对小格子的误判。

#### 第二轮：原生 NCC 模板匹配

```
从第一轮结果构建原生模板（每个数字取样本均值）
  → 对每个单元格做 NCC 匹配
  → 信任层次：
     极高置信度（>0.50 且 margin>0.05）→ 信任 NCC
     高置信度但与第一轮不一致 → 保护第一轮标签
     中等置信度 → 优先信任第一轮
     低置信度 → 回退第一轮
```

关键设计：原生模板门槛降至 `>=2` 个样本即可构建（原为 3），避免小样本数字回退到合成模板后 NCC 分数不敌大类模板。

---

## 性能优化详解（CSP 求解器）

针对 15×15 及以上大图，求解器在以下方面进行了深度优化：

### 1. 增量数据结构

| 结构 | 类型 | 作用 | 优化前 | 优化后 |
|---|---|---|---|---|
| `_unknown_edges` | `set[tuple]` | 维护所有未知边 | `find_unknown()` 全盘扫描 | 仅迭代未知边集合 |
| `_line_count` | `int` | LINE 边的总数 | `_check_cycle()` 每次遍历 | O(1) 读取 |

两者在 `set()` / `restore_state()` 中自动增量更新，支持状态回滚。

### 2. 改进的分支启发式（`_edge_priority`）

为每条未知边计算优先级分数：

| 因素 | 权重 | 说明 |
|---|---|---|
| 与编号格子相邻 | +20 | 有数字的格子更能触发约束传播 |
| 紧迫度 | +(`urgency` × 15) | `(num - line_count) / unknown`：剩余需填 LINE 占比越高越优先 |
| 已知信息量 | +(`known` × 5) | 相邻格子中已确定的边越多，推断越可靠 |
| 0 或 3 格子 | +10 | 极端数字的约束力更强 |
| 端点有 1 条 LINE | +30 | 可能延续回路，优先级最高 |
| 端点有 2 条 LINE | −50 | 度数已满，该边只能是 CROSS |

### 3. 脏标记增量传播

`_dirty_cells` 和 `_dirty_dots` 集合只记录受最近 `set()` 影响的格子和点，避免全盘扫描。传播复杂度从 O(RC) 降为 O(受影响单元数)。

### 4. 可回滚并查集（`RollbackDSU`）

- 支持 `save()` / `restore()` 操作，回溯时 O(1) 恢复。
- 操作记录在 `_trail` 中，无需深拷贝整个棋盘。

### 5. 状态去重缓存（`_dead_cache`）

- 将完整棋盘状态序列化为 `bytes`（每条边 1 字节）。
- 已探明无解的状态直接剪枝，避免重复搜索子树。

### 6. 对角线 0-3 模式

相邻对角格子分别为 0 和 3 时，可以确定大量边。此优化将部分 15×15 谜题的推理步骤从 ~200 步降至 ~50 步。

---

## 应用到的算法与技术

| 算法/技术 | 应用于 | 说明 |
|---|---|---|
| 约束传播 | CSP 求解器 | 数字、度数、局部模式快速推导 |
| 回溯搜索（DFS） | CSP 求解器 | 假设-验证，含死状态缓存剪枝 |
| SAT 编码（布尔逻辑） | SAT 求解器 | 变量对应边状态，子句对应约束 |
| Glucose3 SAT 引擎 | SAT 求解器 | CDCL 算法，大规模高效求解 |
| 基数约束编码（CardEnc） | SAT 求解器 | 顶点度数 ≤ 2 的 AtMost 约束 |
| 回路消除迭代 | SAT 求解器 | 逐步禁止短回路直至单回路 |
| 可回滚并查集 | CSP 求解器 | O(1) 环路检测与回溯恢复 |
| 启发式分支排序 | CSP 求解器 | 综合紧急性、线连续性评分 |
| 脏标记增量更新 | CSP 求解器 | O(受影响单元) 替代 O(RC) |
| 归一化互相关（NCC） | 图片识别 | 模板匹配数字分类 |
| 连通组件分析 | 图片识别 | 黑点/数字笔画分离 |
| Otsu 自适应阈值 | 图片识别 | 全局 + 逐格二值化 |
| K-means 聚类 | 图片识别 | 网格线拟合（坐标聚类） |
| BFS / DFS | CSP 求解器 | 最终解验证：单一回路检查 |

---

## 完整测试结果

以下为当前版本在所有测试谜题上的端到端结果（识别 + SAT 求解）：

| 谜题 | 尺寸 | 网格数 | 数字分布 | 求解耗时 |
|---|---|---|---|---|
| `5_1.png` | 5×5 | 25 | 1:2, 2:4, 3:7 | 0.013s |
| `10_1.png` | 10×10 | 100 | 1:12, 2:11, 3:18 | 0.004s |
| `15_1.png` | 15×15 | 225 | 0:3, 1:25, 2:44, 3:27 | 0.008s |
| `15_2.png` | 15×15 | 225 | 0:2, 1:14, 2:43, 3:30 | 0.008s |
| `20_1.png` | 20×20 | 400 | 0:9, 1:26, 2:67, 3:56 | 0.020s |
| `25_1.png` | 30×25 | 750 | 0:6, 1:73, 2:158, 3:95 | 0.048s |
| `July.png` | 50×40 | 2000 | 0:37, 1:258, 2:368, 3:203 | 0.180s |

---

## 可扩展方向

- 🔬 **更多验证过的高级模式**：边界上的 1-3 相邻、2-2 对角等局部定式。需充分测试避免错误推断。
- 🌲 **分支策略优化**：尝试 conflict-directed backjumping 减少无效回溯。
- 🎨 **图片识别增强**：支持倾斜校正、透视变换、多种字体风格。
- 🔄 **交互式编辑**：GUI 中支持点击输入数字、手动标记边，实时验证。
- 📱 **Web 界面**：Flask / FastAPI 后端 + 前端渲染，支持在线求解。
- 🏆 **更大规模**：探索 MaxSAT 或 ILP 编码处理 100×100 级别的谜题。
